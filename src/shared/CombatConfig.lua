-- CombatConfig.lua
-- Shared constants for combat logic (both server and client).
-- Reduces magic numbers and keeps attack ranges in sync.
return {
	PET_DETECTION_RANGE = 30,
	PET_ATTACK_RANGE = 15,
}
